請把下面整段貼到新的 ChatGPT 對話：

我正在做一個 iPhone-only 的 Pikmin Bloom 自動化 App，名字叫「Pikmin Pilot」。

最終目標：
- 給一般使用者一個 App / IPA。
- 平常使用完全不接 Windows、不跑 CMD、不開 SideTap。
- App 自己在 iPhone 上辨識 Pikmin Bloom 畫面並操作：
  找可用水果 → 點進探險 → 選粉紅 → 選 12 隻 → GO → 關閉 X → 回列表 → LOOP。
- 我沒有 Mac；用 GitHub Actions macOS runner build unsigned IPA，再用 Sideloadly 簽/裝。

已經證明成功的部分：
1. Windows + WDA 版本的完整 bot 流程已成功。
2. Stage 5 iOS Controller 經 localhost WDA 可完整單次派遣，也可連續 LOOP。
3. Stage 5 的水果辨識語意是 card-first：
   - 整張粉紅/時間/進度卡 = BUSY，不點
   - 整張綠色「完成！/領取！」卡 = COMPLETE，不點
   - 只有沒有狀態卡的普通水果 = AVAILABLE
4. 粉紅 Pikmin 固定選 12 隻；GO、攜帶畫面綠色 X、循環都已成功。
5. Stage 6 已證明：
   - 普通 iOS App 本身不能直接成為 XCTest host。
   - 自製 XCUITest Runner 經 go-ios/testmanagerd 啟動後，localhost:8200 ONLINE。
   - Runner 可以用 XCUIApplication 成功叫出 Pikmin。
   - 但這條 Runner 路線仍需要電腦啟動，所以不當成品架構。
6. 現在已改走 Stage 7「手機本機 idevice / Remote Pairing / RSD / CoreDevice」架構。
7. Stage 7.0.2 的 Pikmin Pilot App 已可正常安裝啟動，顯示 embedded engine 尚未連接。
8. GitHub Actions 已成功 build 出 IDevice.xcframework。這份 XCFramework 已提供給上一個 ChatGPT：
   - ios-arm64/libidevice_ffi.a
   - ios-arm64/Headers/idevice.h
9. 上一個 ChatGPT 已做 Stage 7.1，把 IDevice.xcframework 直接 vendor 進 Pikmin Pilot，並加了 Objective-C bridge。

Stage 7.1 真正呼叫的 idevice FFI：
- rp_pairing_file_read
- rp_pairing_file_to_bytes
- tunnel_create_rppairing
- rsd_get_uuid
- app_service_connect_rsd
- app_service_launch_app("com.nianticlabs.pikmin")

Stage 7.1 App 內有三個主要測試：
1. 匯入 RPPairing Record
2. VALIDATE WITH IDEVICE
   預期：IDEVICE LINKED • RPPairing OK • ... bytes
3. CONNECT PHONE-LOCAL RSD
   目前測試 target：10.7.0.1:49152
   預期：PHONE-LOCAL RSD ONLINE • UUID ...
4. PHONE-LOCAL → LAUNCH PIKMIN
   預期：不靠 Windows，Pikmin Bloom 被叫到前景。

重要：
- Stage 7 不准偷偷 fallback 到 WDA localhost:8100。
- 最終成品不要要求 Windows runtime。
- iPhone 是 iOS 26.6.1。
- 現階段最重要的是把手機端 transport 接通，再把 Stage 5 的 screenshot/tap/swipe 搬到 phone-local engine。
- 不要再把主要時間花在 Windows Runner；那條只作為已證明的技術參考。
- 我偏好繁體中文、直接給測試步驟與可下載 ZIP，不要一直講抽象限制。

請從 Stage 7.1 繼續，若 build/test 有錯誤就直接修下一版。
